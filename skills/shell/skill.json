{
  "name": "shell",
  "displayName": "Shell",
  "description": "Execute shell commands on macOS and return stdout/stderr",
  "version": "1.0.0",
  "type": "tool-provider",
  "configSchema": {
    "timeout": { "type": "number", "default": 10000, "label": "Command timeout (ms)" }
  },
  "tools": [
    {
      "name": "run_shell_command",
      "description": "Executes a shell command on macOS and returns stdout/stderr.",
      "args": { "command": "string" }
    }
  ],
  "dependencies": []
}
